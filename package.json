{
  "name": "yrkesskade-saksbehandling",
  "version": "1.0.0",
  "description": "",
  "main": "server.ts",
  "scripts": {
    "start": "node dist/server.js",
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "rimraf dist && ncc build server/server.ts -o dist",
    "build-prod": "rimraf build && cd client && yarn && yarn run build && cd .. && yarn && yarn run build",
    "server": "nodemon --verbose --watch \"server\" --ext \"ts,json\" --exec ts-node server/server.ts",
    "client": "cd client && yarn start && cd ..",
    "dev": "concurrently \"yarn run client\" \"yarn run server\"",
    "docker": "yarn build-prod && docker build -t saksbehandling .",
    "docker-run": "docker run -dp 3000:3000 saksbehandling"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/navikt/yrkesskade-saksbehandling.git"
  },
  "author": "Team Yrkesskade",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/navikt/yrkesskade-saksbehandling/issues"
  },
  "homepage": "https://github.com/navikt/yrkesskade-saksbehandling#readme",
  "dependencies": {
    "@hookform/resolvers": "^2.8.5",
    "@navikt/bedriftsmeny": "^3.5.2",
    "@navikt/ds-icons": "^0.7.1",
    "@navikt/nav-dekoratoren-moduler": "^1.6.9",
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "js-cookie": "^3.0.1",
    "yup": "^0.32.11"
  },
  "devDependencies": {
    "@types/express": "^4.17.13",
    "@types/node": "^16.11.12",
    "@vercel/ncc": "^0.31.1",
    "concurrently": "^6.4.0",
    "nodemon": "^2.0.15",
    "rimraf": "^3.0.2",
    "ts-node": "^10.4.0",
    "tslint": "^6.1.3",
    "typescript": "^4.5.4"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  }
}
